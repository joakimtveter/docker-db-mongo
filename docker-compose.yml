name: MongoDB Server
services:
  mongodb:
    container_name: mongoDB
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: joakim1234
    restart: unless-stopped
